# Dimension Bridge Environment Configuration

# =============================================================================
# IMAGE CONFIGURATION (REQUIRED)
# =============================================================================
# This image should be provided by your administrator
# DO NOT change this unless instructed by your DevOps team
DIMENSION_BRIDGE_IMAGE=dimension-bridge:v1.0.0

# =============================================================================
# CERTIFICATE SERVICE CONFIGURATION (REQUIRED)
# =============================================================================
# Primary server IP or domain for certificate
SERVER_IP=your-service.company.internal

# Service identifier for certificate files and logging
SERVICE_NAME=cert-agent

# =============================================================================
# STEP CA CONFIGURATION (REQUIRED if using Step CA)
# =============================================================================
# Step CA server URL
STEP_CA_URL=https://ca.company.internal:9000

# Step CA root certificate fingerprint (get from administrator)
STEP_CA_FINGERPRINT=your-ca-fingerprint-here

# =============================================================================
# CERTIFICATE LIFECYCLE SETTINGS (OPTIONAL)
# =============================================================================
# How often to check certificate status (seconds)
CHECK_INTERVAL=21600

# Renew certificate when this many days remain
DAYS_BEFORE_RENEWAL=7

# Certificate validity period (days)
CERT_VALIDITY_DAYS=30

# =============================================================================
# SERVICE INTEGRATION (OPTIONAL)
# =============================================================================
# Command to execute after certificate renewal
# Examples:
#   - docker exec nginx nginx -s reload
#   - curl -X POST http://api:8080/reload-ssl
#   - docker restart my-service
RELOAD_COMMAND=

# =============================================================================
# NOTIFICATIONS (OPTIONAL)
# =============================================================================
# Slack webhook URL for notifications
SLACK_WEBHOOK_URL=

# =============================================================================
# DEBUGGING AND LOGGING (OPTIONAL)
# =============================================================================
# Log level: trace, debug, info, warn, error
RUST_LOG=info
